/** @type {import('next').NextConfig} */
const nextConfig = {
    basePath: '/gp',      // dairy sacco graders portal tells Next.js this app lives under /dairy
    assetPrefix: '/gp',   // optional: ensures static assets load correctly
    reactStrictMode: true
  };
  
  module.exports = nextConfig;
  
  